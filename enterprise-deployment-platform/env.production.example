# Production Environment Configuration Template
# Configure these in Vercel dashboard for production environment
# DO NOT commit actual values to version control
# Use strong, unique secrets for production

# Environment
NODE_ENV=production

# Application
NEXT_PUBLIC_APP_URL=https://yourdomain.com
NEXT_PUBLIC_APP_NAME=Enterprise E-commerce Platform

# Sentry Error Tracking (REQUIRED for production)
NEXT_PUBLIC_SENTRY_DSN=your_production_sentry_dsn_here
SENTRY_AUTH_TOKEN=your_production_sentry_auth_token_here
SENTRY_ORG=your_sentry_org
SENTRY_PROJECT=your_sentry_project_production

# Database (if needed)
# DATABASE_URL=postgresql://production-db-host:5432/enterprise_production

# API Keys (REQUIRED for production)
# STRIPE_SECRET_KEY=sk_live_... (use live keys for production)
# PAYMENT_GATEWAY_API_KEY=production_key_...

# Security (REQUIRED - use strong, unique secrets)
# JWT_SECRET=production-jwt-secret-min-32-chars-rotate-every-90-days
# ENCRYPTION_KEY=production-encryption-key-min-32-chars-rotate-every-90-days

# Application Start Time (for uptime tracking)
# START_TIME is set automatically by deployment platform

# Performance Monitoring
# Enable Vercel Analytics and Speed Insights in Vercel dashboard
